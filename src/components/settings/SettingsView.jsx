import { navItems } from '../../data/constants'
export default function SettingsView({ setActiveNav, announce, notice }) { return <div className="app-shell" data-notice={notice}><aside className="sidebar"><div className="brand"><span className="brand-mark">S</span><span>Study<span className="brand-accent">Manager</span></span></div><div className="profile-card"><div className="avatar">JP</div><div><strong>Juan Perez</strong><span>Estudiante · 4to semestre</span></div></div><nav aria-label="Navegacion principal"><span className="nav-label">Workspace</span>{navItems.map(([icon, label]) => <button key={label} className="nav-item" onClick={() => setActiveNav(label)}><span className="nav-icon">{icon}</span>{label}</button>)}</nav><div className="sidebar-bottom"><button className="nav-item active"><span className="nav-icon">⚙</span>Configuracion</button></div></aside><main className="main-content"><header className="topbar"><div className="breadcrumb">Workspace <span>/</span> Configuracion</div></header><div className="section-view"><div className="section-view-header"><p className="eyebrow warm">WORKSPACE / CONFIGURACION</p><h1>Configuracion</h1><p className="subtitle">Personaliza las preferencias de tu espacio de estudio.</p></div><section className="panel settings-panel"><h2>Preferencias</h2><label className="setting-row"><span>Mostrar resumen al iniciar</span><input type="checkbox" defaultChecked /></label><label className="setting-row"><span>Recordatorios de tareas</span><input type="checkbox" defaultChecked /></label><button className="primary-button" onClick={() => announce('Configuracion guardada')}>Guardar cambios</button></section></div></main>{notice && <div className="toast" role="status">{notice}</div>}</div> }
